# Use the public registry for this repo so `npm install` works without Adobe VPN/certs.
# Overrides a user/global .npmrc that points at artifactory (which caused UNABLE_TO_GET_ISSUER_CERT_LOCALLY).
registry=https://registry.npmjs.org/
